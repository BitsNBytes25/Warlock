meta {
  name: Service Create
  type: http
  seq: 8
}

put {
  url: {{host}}/api/service/:guid/:host/:service
  body: json
  auth: inherit
}

params:path {
  service: {{lookup_service}}
  host: {{lookup_host}}
  guid: {{lookup_app}}
}

body:json {
  {"action": "stop"}
}

settings {
  encodeUrl: true
}
