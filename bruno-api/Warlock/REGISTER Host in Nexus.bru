meta {
  name: REGISTER Host in Nexus
  type: http
  seq: 34
}

post {
  url: {{host}}/api/host/register/:host
  body: json
  auth: inherit
}

params:path {
  host: {{lookup_host}}
}

body:json {
  {"email": "Foo"}
}

settings {
  encodeUrl: true
  timeout: 0
}
